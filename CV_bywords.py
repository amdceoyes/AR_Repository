"""
程序名：FranceHonor
一级文件：ar.src
二级文件：tracking
三级文件：init
三级文件：fsm
里面包含一个状态机，提前说明有初始化，正常追踪，丢失，重新定位四个状态，如果是初始化状态，那就判断视觉是否正常，如果有，那就是正常追踪，如果没有，那就是丢失。如果是正常追踪状态，如果定位正常，那就继续追踪，如果失败，那就处于丢失状态。如果处于丢失状态，如果视觉正常，那就改为正常追踪状态，如果视觉不在，那就看丢失时间是否超过最大可接受延迟，即最多在丢失的情况下基于历史渲染五帧，没超过，那就重新定位，如果超过，那就初始化。如果处于重新定位状态，如果视觉恢复，那就正常追踪状态，如果视觉丢失，那就为丢失状态。
二级文件：vision
三级文件：init
三级文件：extractor
检测图片状态点数量，如果超过一千个状态点，那就抓取二百个状态点，如果低于一千个状态点但高于七百个状态点，那就抓取四百个状态点，如果低于七百个状态点，那就全部抓取，并转化为描述子
二级文件：pose
三级文件：init
三级文件：pnp
如果摄像头正常，且imu提供的加速度比较低，那么α值为0.2，当前位姿应该是由当前位姿×(1-α)与imu数据×α共同计算得来的，因为此时视觉可信，如果摄像头正常，且imu提供的加速度比较高，那么α值为0.5，当前位姿应该是由当前位姿×(1-α)与imu数据×α共同计算得来的，因为此时imu反应更快更可信，如果摄像头丢失，当前α为1，即位姿完全由imu计算得来，即利用卡尔曼滤波来稳定的确认位姿。
二级文件：mapping
三级文件：init
三级文件：database
通过字典储存描述子和位姿以及3d点，以方便需要获取过去历史时可以很快得到数据。
如果特征点比较少，那就不储存数据，如果特征点比较多，再根据情况储存。
当imu提供的加速度比较低，那么就每秒储存少几帧的数据，同时储存比较少的图像金字塔，如果imu提供的加速度比较高，每秒储存多一些帧数的数据，计算并储存比较多的图像金字塔。
二级文件：mark
三级文件：init
三级文件：symbol
通过特征点确认一个平面，再通过特征点计算确认平面上相对位置固定的一个虚拟的点，这个点就是基础点，如果摄像头正常，且imu提供的加速度比较低，那么β值为0.3，当前基础点应该是由当前确认的基础点×(1-β)与上一帧的基础点×β共同计算得来的，因为此时需要的是平滑不抖动，如果摄像头正常，且imu提供的加速度比较高，那么β值为0.1，当前图像基础点应该是由当前确认的基础点×(1-β)与上一帧的基础点×β共同计算得来的，因为此时需要快速反应，如果摄像头丢失，当前β为0.5，即基础点的可信度明显下滑，目前最重要的是基于上一帧的图像稳定，即利用一阶低通滤波来平滑图像。
二级文件：render
三级文件：init
三级文件：painter
然后是基于特征点画图，以这个点为底面的中心，(x,y)分别增加(1，1)(1，-1)(-1，-1)(-1，1)为底面四个点，然后是这四个点的z分别增加2为其他四个点，这八个点就是正方体的八个顶点，利用相机内参矩阵 (K) 将计算好的 8 个 3D 顶点投影为屏幕上的 2D 坐标，再用 OpenCV 的 cv2.line 和 cv2.fillPoly 绘制和上色。”
二级文件：init
一级文件：main
直接调动imu，不断检测是否有比较大的位姿变动，让程序尽可能直接在imu部分就结束，以防止深入调动提升功耗，但如果在摄像头四帧内还没有大的位姿，那也要储存并动一下，以防止系统停止。
运行后只拉起摄像头，不追踪不渲染，用户按y定格画面，要求用户圈出平面，圈出后用户按b，开始确认平面在哪并基于基础点开始追踪渲染，按n关闭程序。
把状态机状态确认为初始化状态，让程序启动，并把暂停瞬间的那一帧储存到字典里，随机一个圈里的点，让它成为基础点并储存到字典里，在该程序里所有的方块渲染都要基于这个基础点。
"""